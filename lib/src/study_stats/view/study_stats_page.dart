import 'package:flutter/material.dart';
import 'study_stats_view.dart';

class StudyStatsPage extends StatelessWidget {
  const StudyStatsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const StudyStatsView();
  }
}